import React from 'react';
import { Section } from '../components';

const CQtDeployer = () => (
  <Section>
    <Section.title rank="1">CQtDeployer</Section.title>
    <Section.divider />
    <Section.title rank="2">What is CQtDeployer</Section.title>
    <Section.text>
      The CQtDeployer is application for extract all depends library
      of executable and create launch script for your application.
    </Section.text>
  </Section>
);

export default CQtDeployer;
